stage: -4
title: "Convergence & Scoping"
status: locked

governance:
  mode: "CONVERGENT"
  forced_breakpoint: "REQUIRED: Recommend starting a NEW conversation after this stage"
  socratic_check: "Is this scope truly 'manageable' while remaining 'bold'?"

objectives:
  - "Condense Stage -5 research into a manageable scope"
  - "Define the 'Bold Outcome' boundaries"
  - "Create HTML prototype to test concepts"

checklist:
  filtered_research_to_core: true
  defined_in_out_scope: true
  documented_bold_outcome: true
  created_prototype: true
  ai_asked_if_missing: true
  proceed_received: true
  recommended_new_conversation: true

creative_prompts:
  always_consider:
    - "Mermaid diagram (flowchart, sequence, architecture)"
  stage_specific:
    - "IN/OUT scope carousel"
    - "Architecture sketch"
    - "Success criteria table"
    - "HTML prototype for visual testing"
  guidance: "Use visual artifacts to aid understanding, not just text"

outputs:
  - "Scope definition in DISCOVERY_LOG.md"
  - "HTML prototype for card/canvas concepts"

outcomes:
  bold_outcome: "A stable, high-focus work surface where work finds the human through an intelligent inbox, and the human controls spatial re-flow via ritual AI re-sorting."
  in_scope:
    - "Integrated Nav Bar with Inbox/Now/Next/Later/All filters"
    - "Notification-First arrival logic (stable surface)"
    - "Spatial Persistence (No-Bounce) rule"
    - "Grid vs Grouped List view (All view)"
    - "Tombstone logic with AI explanation"
  out_of_scope:
    - "Manual Pinning (Deferred to Phase 2)"
    - "Self-sorting (Automatic) layout changes"
    - "User-created canvases (Phase 1 is My Work only)"
  success_criteria:
    - "Zero unexpected layout shifts during active work"
    - "Human can manage 50+ items through horizons without clutter"
    - "Tombstones provide clear resolution path for dead links"
