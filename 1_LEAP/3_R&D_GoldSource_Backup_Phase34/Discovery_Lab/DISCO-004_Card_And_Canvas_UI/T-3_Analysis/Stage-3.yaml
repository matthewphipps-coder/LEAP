stage: -3
title: "Analysis & Risk"
status: locked

governance:
  rigor: "Deep Dive Research"
  socratic_check: "What is the one thing that could make this fail?"

objectives:
  - "Perform technical deep dive into chosen scope"
  - "Identify risks, performance impacts, and security concerns"

checklist:
  technical_research_performed: true
  risk_registry_created: true
  feasibility_assessed: true
  ai_asked_if_missing: true
  proceed_received: true

creative_prompts:
  always_consider:
    - "Mermaid diagram (flowchart, sequence, architecture)"
  stage_specific:
    - "Risk matrix table"
    - "Sequence diagrams"
    - "Flow diagrams"
  guidance: "Use visual artifacts to aid understanding, not just text"

outputs:
  - "Risk assessment notes in DISCOVERY_LOG.md"

outcomes:
  risks:
    - "Manual vs AI jitter (Mitigated by User-only re-sort triggers)"
    - "Data sync latency (Mitigated by Inbox/Updated flag awareness)"
  technical_decisions:
    - "card_id is the primary key for conversation logs"
    - "SoR-agnostic JSON schema with local-first content support"
    - "State Machine based on Now/Next/Later/Done + Inbox boolean"
  dependencies:
    - "Nexus Conversation Store (Persistent log service)"
