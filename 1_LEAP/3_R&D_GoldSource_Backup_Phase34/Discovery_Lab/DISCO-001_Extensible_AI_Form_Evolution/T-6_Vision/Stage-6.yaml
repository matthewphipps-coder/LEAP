stage: -6
title: "The DreamBig Idea!"
status: locked

governance:
  brake_type: "Standalone !Proceed"
  socratic_check: "Did I ask 'Why?' or 'What if?' about the vision?"

objectives:
  - "Capture the user's raw 'DreamBig' vision"

checklist:
  ai_asked_for_vision: true
  user_provided_intent: true
  ai_confirmed_understanding: true
  ai_asked_if_missing: true
  proceed_received: true

creative_prompts:
  always_consider:
    - "Mermaid diagram (flowchart, sequence, architecture)"
  stage_specific:
    - "Carousel of inspirations"
    - "Vision statement card"
  guidance: "Use visual artifacts to aid understanding, not just text"

outputs:
  - "DISCOVERY_LOG.md initialized"
  - "Stage -6 summary in DISCOVERY_LOG.md"

outcomes:
  vision_summary: "NEXUS becomes an AI-native workbench where conversation drives action and forms evolve into human-AI collaboration surfaces"
  
  key_decisions:
    - "AI is the new UI - but users still need to SEE things"
    - "Forms should orient to human/AI collaboration, not just display fields"
    - "Agents do the work, humans collaborate and guide"
    - "Must work across ServiceNow's breadth (ITSM, HR, CSM, Field Service)"
  
  carry_forward:
    - dynamic_field_visibility: "AI can change what fields are visible based on conversation"
