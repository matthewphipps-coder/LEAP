stage: -6
title: "The DreamBig Idea!"
status: locked

governance:
  brake_type: "Standalone !Proceed"
  socratic_check: "Did I ask 'Why?' or 'What if?' about the vision?"

objectives:
  - "Capture the user's raw 'DreamBig' vision"

checklist:
  ai_asked_for_vision: true
  user_provided_intent: true
  ai_confirmed_understanding: true
  ai_asked_if_missing: true
  proceed_received: true

creative_prompts:
  always_consider:
    - "Mermaid diagram (flowchart, sequence, architecture)"
  stage_specific:
    - "Carousel of inspirations"
    - "Vision statement card"
  guidance: "Use visual artifacts to aid understanding, not just text"

outputs:
  - "DISCOVERY_LOG.md initialized"
  - "Stage -6 summary in DISCOVERY_LOG.md"

outcomes:
  vision_summary: "The canvas is the human-centric view of work"
  requirements_count: 11
  key_decisions:
    - "Design for pluggable renderers (C), build single canvas (A)"
    - "Hybrid ownership: My Work default + user-created later"
    - "Data-driven filters (Option 3)"
    - "Dynamic card sizing based on relative importance"
    - "Source-agnostic importance factors (AI or field mapping)"
    - "Match workbench aesthetic"
  carry_forward:
    - "11 requirements (R1-R11)"
    - "Work flow directions (To/From/With/About Me)"
    - "Canvas data model with filter criteria"

